package examples.correct.lecture;
sequencediagram example_pretty {
  kupfer912: Auction;
  bidPol: BiddingPolicy;
  timePol: TimingPolicy;
  a -> b : methodenname(arg);
  f -> BidMessage bm = new BidMessage(...);
  class A -> b : test();
  a -> b : test() {
    a -> b : test();
    b <- a : test();
  }
}
